<% if logged_in? %>
  <span><%= current_user.name %></span> <a href="root"><%= link_to "Log out", logout_path, method: :delete %></a>
<% else %>
  <a href="signup">Sign up</a>    <a href="login">Log in</a>
<% end %>